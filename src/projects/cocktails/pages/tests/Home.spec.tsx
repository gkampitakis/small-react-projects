export {};

describe('Home', () => {
  beforeEach(jest.resetAllMocks);

  it.todo('Should render correctly');

  it.todo('Should render loading prompt');

  it.todo('Should not render loading prompt');

  it.todo('Should render error prompt');
});
